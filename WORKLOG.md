# 작업 일지

> 세션이 바뀌어도 여기만 읽으면 **어디까지 했고 다음에 뭘 할지** 알 수 있게 유지한다.
> 최신 항목이 위로 온다. 각 세션 끝에 「다음 할 일」을 반드시 갱신할 것.

---

## 재시작 절차 (컴퓨터를 껐다 켠 뒤)

디스크에 남는 것: `scripts/` 도구 4종, 이 파일, codex CLI(`~/.local/bin/codex`)와
로그인(`~/.codex/auth.json`), ollama 모델. **다시 띄워야 하는 것은 아래뿐이다.**

```bash
# 1) cmux 레이아웃 복구 (4분할: Claude / 브라우저 / codex / ollama)
cmux restore-session

# 2) 백엔드 — 저장소 루트의 .venv 를 쓴다
cd green-retrofit-backend && ../.venv/bin/python -m uvicorn main:app --host 127.0.0.1 --port 8000

# 3) 프론트 — 반드시 5173 을 잡아야 한다 (ALLOWED_ORIGINS 기본값이 5173뿐)
cd green-retrofit-frontend && npm run dev

# 4) 브라우저 패널
cmux open http://localhost:5173
```

**패널 세션은 수동으로 다시 띄워야 한다.** 좌하단 터미널에서 `codex`,
우하단에서 `ollama run gemma4:e4b`. 이게 떠 있지 않으면 릴레이 스크립트의
패널 자동 탐지가 실패한다(탐지는 화면 내용으로 하므로 surface 번호가 바뀌어도 무방).

릴레이 사용법:
```bash
./scripts/review-loop.sh <질문파일.md> <주제>      # 코드 검토 루프 (1~2회 codex, 3회차 Gemini 중재)
./scripts/relay-cmux.sh "무엇을 바꿨는지 브리핑"    # 커밋 안 된 변경 검토 (codex→gemma)
./scripts/ask-panels.sh --codex-only <질문파일.md> # 자유 질문
```
중재는 `relay-gemini` 전용 패널을 자동 생성해 거기서 돈다. `GEMINI_API_KEY` 가
설정돼 있어야 한다(개인 무료 티어는 거절됨 — AI Studio 키 사용).
`.relay/` 는 gitignore 대상이라 재부팅 후 비어 있을 수 있다 — 라운드 카운터가
초기화될 뿐 문제 없다.

**사라지는 것**: 실행 중이던 서버, 패널 세션, `/private/tmp` 의 스크래치패드
(EPlusSimple 클론, 이전 릴레이 기록 보관분). 전부 재생성 가능하다.
EPlusSimple 비교가 다시 필요하면:
`git clone --depth 1 --branch V0-6-3 https://github.com/snu-bslab/EPlusSimple.git`

---

## 운영 규칙

**Claude 또는 codex 중 하나라도 일일 사용 한도에 도달하면 그 즉시 작업을 멈춘다.**
재시도·우회 금지. 남은 일은 이 파일에 적고 종료한다. (과금 방지)
`scripts/relay-cmux.sh` 에 구현돼 있다 — 패널에서 한도 신호 감지 시 exit 99.

---

## 지금 상태 요약 (2026-08-13 기준)

커밋 `7e314cb`. 브랜치 `fix/review-round2-geometry` — **main 미병합.**
(`7f28b32`·`ee931c7` 는 이미 main `a844170` 에 들어와 있다.)
백엔드 **747 passed + 3 xfailed**. 골든 IDF 불변.

세션 전체 요약은 저장소 루트 `working.md` 및 구글 드라이브 사본에 있다:
https://drive.google.com/file/d/16FyeJA4ATWUVWJpDNQG6i8YDntPzaR_x/view

### 이번에 고친 것

| 커밋 | 내용 | 영향 |
|---|---|---|
| `7f28b32` | `WindowShadingControl` IDD 확장 필드 — 창 10개 초과 존에서 시뮬레이션이 통째로 죽던 문제 | 회의실 9개 존(최대 251창) 복구 |
| `ee931c7` | gbXML 이 **선언한** 지면 접촉면(`SlabOnGrade` 등)을 경계조건 판정에서 안 읽던 문제 | 운동시설 난방 39.61→**23.48** (−40.7%), 회의실 18.83→**17.27** (−8.3%) |

⚠️ `RaisedFloor` 는 지면 접촉 목록에서 제외했다 — 이건 지금도 맞다.
   **다만 "그러니 외기 노출이 맞다"까지 간 건 틀렸다**(아래 round-2 참조).
⚠️ `promoteGroundFloors`(추정, opt-in)와 성격이 다르다 — 이건 gbXML 이 **선언**한 사실이다.

### codex round-2 검토 완료 (`e573c88`, 브랜치 `fix/review-round2-geometry`)

밀려 있던 `7f28b32`·`ee931c7` 검토를 돌렸고 **실질 결함 3건**이 나와 고쳤다.
**724 passed + 3 xfailed**, 골든 IDF 불변, 실제 파일 4개 선언지면 판정 불변.

| 결함 | 왜 안 보였나 |
|---|---|
| `\min-fields` 가 확장 필드 블록을 밀었다 (`BuildingSurface:Detailed` 9칸, `Schedule:Compact` 3칸) | 확장 객체를 `WindowShadingControl` 하나만 썼고 그게 `min-fields 0` 이라 우연히 맞았다 |
| `UndergroundCeiling` 이 파서에서 `Ceiling` 으로 뭉개져 Ground 분기 도달 불가 | 시험이 payload 에 타입을 직접 넣어 파서를 건너뛰었다 |
| 선언(`GROUND_CONTACT_TYPES`)이 opt-in 추정보다 **뒤**라 `ground_promoted` 로 오기록 | 결과 IDF 가 같아 카운터·로그만 거짓이었다 |

`test_raised_floor_stays_outdoors` 는 **틀린 정책을 고정하고 있어** 폐기했다 —
아래 0번의 전제가 여기서 나왔다.

### 짝없는 층간 바닥 규칙 — **완료** (`ffd3b59`)

gbXML 이 인접을 안 적었지만 위·아래에 실이 실재하는 바닥·천장이 `Outdoors +
SunExposed + WindExposed` 로 떨어져, 건물 안쪽 층간 슬래브가 겨울 외기와 햇빛을
받고 있었다.

**회의실.xml 전/후 실측** (연간, 면적 6,539.73㎡ 기준):

| | 난방 kWh/㎡ | 냉방 kWh/㎡ | 합계 |
|---|---|---|---|
| 전 | 19.96 | 17.06 | 37.02 |
| 후 | **14.75** (−26.1%) | 17.80 (+4.3%) | **32.55** (−12.1%) |

⚠️ **예전 추정치 −41.3% / +9% 는 과했다.** 그건 짝없는 바닥 64면을 **전부**
뒤집는다는 전제였는데, 실제로 전환된 건 **37면뿐**이다. 나머지 27면은 하부 층
윤곽이 그 아래까지 뻗지 않는 **셋백/오버행**이라 진짜로 아래가 비어 있다
(`su-47`: 바닥 x −19.3~−14.5, 아래 층 존들은 x ≤ −19.3 에서 끝난다).
비율(26.1/41.3 ≈ 63%)이 전환 비율(37/64 ≈ 58%)과 맞아떨어진다.

**타입만 보고 뒤집지 않는다.** 기하로 세 가지를 **모두** 확인한다 —
① 반대편에 존이 실재 ② 맞닿는 끝이 한 층(3.0m) 이내 ③ XY 투영 절반 이상 겹침.
z 부호만 봤으면 위 27면을 전부 잘못 뒤집었다.

| 파일 | 짝없는 바닥 | 전환 | 나머지 |
|---|---|---|---|
| 회의실 | 64 | **37** | 27 = 셋백/오버행 |
| ARK | 100 | **75** | 16 = 최하층, 1 = 표본 오검출(정확 교차로 교정) |
| 용호동·운동시설 | — | **0** | 골든 IDF 불변 |

**선언은 건드리지 않는다.** `Roof` 는 하늘 노출을, `ExteriorFloor` 는 외기를,
`SlabOnGrade` 는 지면을 **선언**한 것이다. `Floor` 만 아무것도 선언하지 않아
추론 대상이다.

⚠️ 저신뢰 fallback 이라 `assumptions.interstitial_floors`(confidence low)로
사용자에게 보인다. 존 윤곽은 **볼록 껍질 근사**라 ㄱ자 평면 안뜰까지 덮는다 —
오차가 단열 쪽으로 치우친다(반대 방향 오류가 훨씬 커서 받아들였다).

### codex round-2 검토 완료 (`b11b0d1`·`7d57c81`·`7e314cb`)

`ffd3b59` 를 올린 뒤 검토를 돌렸고 **결함 3건 + 쟁점 1건**이 나왔다.

| 결함 | 내용 |
|---|---|
| **[높음]** `Ceiling` → EnergyPlus `Wall` | `_ep_type()` 에 천장 분기가 없어 **수평 천장이 벽으로 나갔다.** `ee931c7` 의 `UndergroundCeiling` 도 `Ground` **벽**이었다. 실제 파일 4개엔 `Ceiling` 이 없어 골든 IDF 로는 영원히 안 잡힌다 |
| 겹침을 격자 표본으로 계산 | "존 윤곽이 오목해서"라는 내 근거가 **틀렸다** — 두 인자 모두 볼록 껍질을 거치므로 항상 볼록이다. 정확 교차면적으로 교체하니 **ARK 76 → 75** (표본이 과대 검출하던 면) |
| 카운터가 명시 지정을 무시 | 판정 분기 안에서 세고 나중에 `boundaryCondition` 을 덮어썼다. round-1 의 `ground_promoted` 지적과 **같은 종류의 오류** |

**쟁점 — `INTERSTITIAL_MAX_GAP = 3.0` 이 너무 크다 (값은 유지)**

0.3m 로 좁히면 회의실 전환이 37 → **0**, 난방 −26.1% 가 통째로 사라진다.
그 띠에 무엇이 있는지 세어 결론을 냈다:

```
11.43~13.51m 띠에 걸친 면 → Shade 93면 (수직 92면 1,532㎡ = 파사드)
```

**건물 외피가 그 띠를 지나간다.** 익스포터가 파사드를 존 없는 `Shade` 로
내보냈고, 존 벽은 재실 높이(2.41m)까지만 올라온 것뿐이다
(층 간격 3.64m − 존 높이 2.41m = 1.2m). 외피 안쪽이라 햇빛·바람이 닿을 수 없다.

다만 "간격이 접촉을 증명하지 않는다"는 지적은 옳아 **신뢰도를 갈라 보고**한다:
회의실 37 (맞닿음 0 / **추정 37**) · ARK 75 (17 / 58). 이웃이 비공조 구역인
경우도 따로 센다(`interstitial_unconditioned`).

### 다음에 바로 할 일

0. **ARK 의 건물 중간 높이 `Roof` 107면** — z 3.43·6.27·9.37·12.47 에 있고 바로
   위에 존이 있다. codex 도 "`Roof` 선언은 유지하되 위 존과 XY 겹침이 크면
   **입력 의미 충돌 warning** 으로 보고하라"고 했다 — 뒤집지 말고 알리는 쪽.
   ⚠️ 착수 전 판단: 경고만 낼지, `severity: block` 급으로 볼지.

1. **볼록 껍질 → 실제 수평 footprint 복수 다각형** (codex 지적, 미반영)
   존 하나가 떨어진 조각으로 나뉘면 그 사이 빈 공간까지 하나의 껍질이 덮는다.
   ㄷ자 중정보다 이쪽이 더 나쁜 실패 양상이다.
2. **이웃 존의 설정온도·스케줄 차이** (codex 지적, 미반영)
   지금은 `isConditioned` 만 본다. 존 단위 스케줄 비교가 필요해 범위가 다르다.
3. **표면 열유속 출력으로 냉방 +4.3% 검증** (codex 제안)
   `Surface Outside/Inside Face Conduction Heat Transfer Rate` 전후 비교.
   ⚠️ codex 지적: **Severe 59→41 은 물리적 정확성의 증거가 아니다.**
   Outdoors 면이 빠지면 그림자 조합에서 빠지는 게 당연하고, 잘못 분류해도 준다.

1. **WWR 90% 상한 → 고정 여백** — 용호동 창면적 8.9% 손실이 0 이 된다.
   EnergyPlus 가 99% 창을 받는 건 실측 확인됨(기술적 제약 아님).
2. **개구부를 호스트 안으로 밀어넣기** — 회의실 397/1,183(34%)이 벽 밖.
   ⚠️ 현재 WWR 축소는 개구부 **자기 중심**으로 줄여서 이걸 못 고친다.
3. **canonical boundary** — `InterZone Surface Tilts/Classes do not match` 용호동 14 · 회의실 4.
4. `severity: block` 을 백엔드 `/api/simulate` 에서도 강제 (지금은 프런트 버튼만 비활성)
5. 지면접촉 선언 타입인데 **유효한 두 존을 함께 가진** 면을 semantic warning 으로
   남기기 (codex 제안 — 지금은 인접 쌍이 이겨서 조용히 지나간다)

**EPlusSimple 식 단순 등가 형상 모드는 보류 결정.** 근거는 `working.md` 5장.
요지: 측정된 최대 오차 두 건이 좌표 품질이 아니라 **인접 의미 해석** 문제라
단순 모드로도 안 풀린다. 없는 면은 다시 그려도 생기지 않는다.

---

## 지난 상태 요약 (2026-08-07 기준)

커밋 `b893cdb`. 브랜치 `fix/afn-infiltration` — 원격 푸시됨(`lactea7/ZeroBase`), **main 미병합**.
백엔드 테스트 **634 passed + 3 xfailed**(남은 xfail 은 ASHRAE 140 공표범위 경계 건).
프런트 lint 0, build 통과.

### 🔥 난방 과소산정 — **원인 2개 확정, 해결** (2026-08-07)

워크로그에 오래 "미해명"으로 남아 있던 건이다. 용호동 8,760시간 실측으로 좁혔다.

**① 설비 가용 스케줄에 재실률을 넣고 있었다** (`5eb4f97`)
`add_unit_heater/add_pthp/add_window_ac/add_ideal_hvac` 에 아키타입 재실률
스케줄(`Op_office`)을 그대로 넘겼다. `Op_office` 는 평일 00~06시·주말 19~06시가
**0.0** 이고 EnergyPlus 의 Availability Schedule 은 0 이면 **장비를 완전히 끈다.**
겨울 01~06시 난방이 정확히 0.0 kWh 였고 실온이 셋백 설정 16℃ 아래인 15.3℃ 까지
떨어졌다 — 모델이 표방한 셋백을 스스로 실행 못 하는 모순.
냉방은 야간 설정이 30℃ 라 손해가 없어 **난방만 편향되게** 깎였다.
→ 설비는 `AlwaysOn`, 제어는 서모스탯이. 조명·기기·사람·급탕은 재실률 유지.

**② 차양이 코드에 아예 없었다** (`4fc9198`)
존 에너지 수지에서 **창 열취득이 모든 항목을 압도**했다. 창 268㎡ · SHGC 0.76 · 차양 0.

⚠️ **정정**: 커밋 `4fc9198` 메시지의 수지 숫자(창 열취득 300,568 / 순취득 203,001
= 235 kWh/㎡)는 **전부 2배로 틀렸다.** eplustbl 의 마지막 행이 `Total Facility`
인데 내 분석 스크립트가 그걸 존 행들과 함께 합산했다(codex 지적).
바른 값은 아래와 같다.

| Total Facility (블라인드 후) | kWh/년 | kWh/㎡ |
|---|---|---|
| 창 열취득 | +100,743 | +116.5 |
| 창 열손실 | −42,101 | −48.7 |
| **순 창 취득** | **+58,641** | **+67.8** |
| 침기 제거 | −48,857 | −56.5 |
| 불투명체 전도 (순) | −28,226 | −32.7 |
| 조명 +기기 +사람 | +45,585 | +52.7 |

블라인드 적용 전 순 창 취득은 117.4 kWh/㎡ 였다 → 67.8 로 **42% 감소**.
결론(창 일사가 지배적)은 그대로지만 크기는 위 값이 맞다.
내부발열 44.5 kWh/㎡(조명 19.4 + 기기 25.1)는 CSV 계산과 일치해 처음부터 맞았다.
원본 gbXML 이 실제로 "Pilkington RW33 이중유리 U2.8566/SHGC0.76" — 파서 오류가
아니라 CAD 내보내기 기본값이다.
→ `WindowMaterial:Blind` + `WindowShadingControl`(창면일사 200 W/㎡).
ASHRAE 140 은 `benchmark.noInteriorBlind` 로 차단(600 vs 610 델타 오염 방지).

|  | 난방 kWh/㎡ | 냉방 kWh/㎡ | 비 |
|---|---|---|---|
| 원래 | 8.87 | 53.87 | 1 : 6.1 |
| ① 후 | 10.58 | 54.09 | 1 : 5.1 |
| **② 후** | **19.01** | 51.29 | 1 : 2.7 |

공급에너지 환산 난방연료 20.0 / 냉방전력 15.5 kWh/㎡ — 한국 공공청사 범위.

**아직 남은 의문**: codex 판정은 **"그 자체로 이상하지 않다"** 였다. 냉방 51.3 은
*열부하*이고 COP 적용 전력은 15.5 인 반면 난방 공급에너지는 20.0 이라, 에너지원
기준으로는 냉방 우위가 아니다. SHGC 0.76 + 사무실 내부발열 + 서울 여름이면
열부하 냉방 우위는 성립한다.

지면 경계는 측정해 봤다(`promoteGroundFloors`): 난방 19.01 → 18.41, 냉방
51.29 → 49.86 으로 **양쪽 다 −3%**. 지중온도(설정온도 −2K)가 겨울 실내보다
따뜻해 오히려 난방이 준다 — 남은 원인이 아니다.

침기 0.5 ACH 는 실측 없이는 방향을 단정할 수 없다(0.3~1.0 에서 9배 변동).

### AFN 침기 결함 — **해결** (2026-08-07, `b893cdb`)

균열 계수를 **㎡ 당**으로 정의하고 표면마다 면적을 곱한 컴포넌트를 만든다.
분할 1/2/4/8 메타모픽 시험 통과(예전엔 8분할에서 난방 +33.5%).
⚠️ 면적을 crack factor 필드에 넣으면 안 된다 — EnergyPlus 가 1.0 이하로 제한한다.

### 이번 세션에서 함께 고친 것

| 커밋 | 내용 |
|---|---|
| `fc41436` | 열모델이 안 바뀌는 단열 대안을 2.5분 재시뮬레이션하던 결함 + 0% 할인율이 5% 로 바뀌던 결함 |
| `9659761` | 대안 평가 실패가 화면에 "에너지 영향 없음"으로 나가던 문제 + 빈 결과가 "전액 절감"으로 계산되던 문제 |
| `bca6b96` | 환기 계수는 kWh/m³ 가 아니라 **SFP** 였다 — "3600 누락"은 내 오진단(넣었으면 3,600배) |
| `236d980` | 단열 대안 U값을 **면별**로 — 프런트와 같은 산식 |
| `b46a93d` | 기준선 판단 → `simulation/baseline.py` |
| `605d41d` | HVAC 모드 → `simulation/hvac_plan.py` |

`ep_simulator.py` 1,909 → **1,570줄**.

### 다음에 바로 할 일

1. codex 검토 회신 반영 (`.relay/heating-and-items-review.md`)
2. 남은 이음매 — 스케줄·내부발열 조립(~150줄), 지오메트리 emit(~250줄).
   codex 조언: 지오메트리 추출 **전에** fake builder 로 `add_surface()` 호출을
   직접 검사하는 시험을 먼저 붙일 것
3. 프런트 리팩터링 (Vitest/jsdom/RTL → pages/reducer 분해). `App.jsx` 1,963줄 / 31 useState
4. `fix/afn-infiltration` → main 병합 판단

---

## 지난 상태 요약 (2026-08-02 기준)

커밋 `15df917`. 백엔드 테스트 **126개**, 프런트 lint 0/0, 빌드 성공.
브랜치 `fix/self-adjacent-surfaces` — **main 미병합, 원격 미푸시**.

---

### AFN 침기 결함 — 조사 1단계 완료, 리팩터링 후 재개 (2026-08-04, 커밋 `13dfd32`)

브랜치 `fix/afn-infiltration`. **구조 리팩터링을 먼저 하기로 해서 여기서 중단.**
재개 시 아래 「남은 일」부터 이어갈 것.

**확정된 결함**: AFN 이 켜진 존에서는 `ZoneInfiltration` 이 시뮬레이션되지 않고
`WallCrack`(0.01/0.65) 계수가 침기를 정하는데, 이 계수가 면적·둘레로 정규화돼
있지 않아 **총 누기가 외피 기밀성이 아니라 표면 개수에 좌우된다.**

메타모픽 실측(ASHRAE 140 케이스 600, 북벽만 분할):

| 북벽 분할 | 외기면 | 난방 MWh | 변화 |
|---|---|---|---|
| 1 | 6 | 6.4972 | — |
| 2 | 7 | 7.0484 | +8.5% |
| 4 | 9 | 7.8030 | +20.1% |
| 8 | 13 | 8.6720 | **+33.5%** |

**용호동 실측 (legacy AFN, 기준값)**

| 항목 | 값 |
|---|---|
| AFN 적용 | 15/20 존 = 바닥면적 93.6%, crack linkage 111개 |
| 건물 평균 실효 침기 | **0.327 ACH** (표기값 0.5 보다 **낮다**) |
| 존별 실효 ACH | 0.08 ~ 1.16 → **최대/최소 14.9배** |
| 난방 (Zone Air System Sensible) | 5,556 kWh = 6.43 kWh/㎡ |
| 냉방 | 49,362 kWh = 57.11 kWh/㎡ |
| 침기 현열손실 | 33,337 kWh = 38.57 kWh/㎡ |

⚠️ **"AFN 은 항상 침기를 과대한다"는 전제는 폐기됐다.** 케이스 600 에서는 1.3~2.0 ACH
(과대), 용호동에서는 0.327 ACH(과소). 외피면수/체적 비에 따라 방향이 뒤집힌다.
따라서 수정 목적은 "침기를 줄이는 것"이 아니라 **"명시한 기밀성과 무관한 형상
의존성을 제거하는 것"** 이다.

**codex 가 정정한 내 오류 2건**
1. **내부발열 44.5 를 "W/㎡ 아키타입 기본값"이라고 한 것 — 두 번 틀렸다.**
   ① 단위가 W/㎡ 가 아니라 **kWh/㎡·년** 이다(2026-08-04 조사로 확정).
   ② 용호동에는 `outletCount` 가 없어 콘센트 부하가 더해지지도 않았다.
   실제 설치밀도는 조명 8.0 + 기기 9.8 = **17.7 W/㎡** 로 정상 범위다.
   자세한 내용은 아래 「미해명 — 난방 과소산정」 절.
2. 44.5 × 8760 ≈ 390 kWh/㎡ 계산은 24시간 100% 가동일 때만 맞다.
   실제로는 `Op_office` 스케줄로 변조되므로 훨씬 작다. EnergyPlus 의 실제 연간
   조명·기기 에너지를 합산해야 한다.

**codex 가 지적한 미완성**: 진단 출력변수는 IDF 에 넣었지만
`_infiltration_assumption()` 에 **실측값이 안 들어간다.** 0.327 ACH·존별 범위·
현열손실은 내가 수동 분석한 것이고 사용자 응답에는 없다. 후처리 집계가 필요하다.
바닥면적 비율보다 **체적 비율**이 침기 해석에 직접적이다(존 높이가 다르면 왜곡).

**남은 일 (재개 지점)**
1. `_infiltration_assumption().detail` 에 실측 집계 추가 (총 침기체적, 체적가중
   평균 ACH, 존별 min/median/max, 현열손실, linkage 수, AFN 적용 **체적** 비율)
2. 화면 요약과 상세 설명 분리 — 요약은 "침기값 신뢰도 낮음 — 실측 기밀성 없이
   AFN 과 고정 ACH 가 혼합 적용되었습니다." 수준으로
3. 메타모픽 시험 추가 — 불변조건: "형상·체적·재료·누기 입력이 같으면 표면 분할만
   바꿔도 총 침기량과 냉난방부하가 변하지 않는다". **수정 코드와 같은 커밋/PR** 에
   넣거나 xfail(strict=True) 로 먼저 넣을 것 (실패 시험만 병합 금지)
4. 기본 모델을 건물 단위 `ZoneInfiltration` 으로 전환, `fixed-ach-v2`.
   기본 0.5 ACH 를 "현재와 비슷한 값"이 아니라 **"건물 전체 자연침기 목표 가정값"**
   으로 정의하고 0.3/1.0 민감도 병행
5. 용호동 재측정 (0.3/0.5/1.0 + legacy 비교)
6. **병행 가능**: 용호동 내부발열 출처 확인 — 존별 조명·기기 입력이 명시값인지
   아키타입인지 콘센트인지, `outletLoadType="sum"` 이중계산 여부

---

### ASHRAE 140 벤치마크 — Tier A 도입 완료 (2026-08-02, 커밋 `a992944`)

`green-retrofit-backend/tests/ashrae140/` — **자세한 내용은 그 안의 `README.md` 가 정본이다.**

**왜 붙였나**: 결과가 이상할 때마다 후보를 손으로 지워온 상태를 끝내려고.
(면적 기준 → 지면 열손실 → 내부발열 → 기상 파일을 다 지웠는데 난방 문제가 남았다)

**Tier A** = 표준 케이스 IDF → 우리 EnergyPlus 25.2 직접 실행. **우리 코드를 타지 않는다.**
나중에 **Tier B**(케이스를 우리 payload 로 → `generate_idf_and_simulate()`)가 실패했을 때
결함이 우리 번역 계층에 있다는 걸 확정할 **대조군**이다.

| 항목 | 우리 | 공표 범위(6종) | NREL 25.2.0 |
|---|---|---|---|
| 600 난방 | 4.2201 MWh | 3.993~4.504 ✅ | 4.325 (−2.4%) |
| 600 냉방 | 6.2592 MWh | 5.432~6.162 ❌ | 6.042 (+3.6%) |

케이스 하나 실행 **약 1초**. 전체 스위트 128 passed + 1 xfailed.
`pytest -m "not slow"` 로 제외 가능.

**냉방 초과는 허용오차를 늘리지 않고 `xfail` 로 남겼다** — 통과하게 맞춘 벤치마크는
가치가 없다. 케이스 사양(200W/0.5ACH/20-27℃/12㎡창/FullInteriorAndExterior)은 전부
일치하고 타임스텝 1~60 을 다 돌려도 설명되지 않는다. 엔진이 같으므로(둘 다 25.2.0)
차이는 **모델**에 있다 — 우리 stock IDF 는 9.3 세대 버전 전이본, NREL 값은
OpenStudio 3.11 measure 생성본. 해소하려면 OpenStudio CLI(약 1GB) 도입이 필요하다.

⚠️ **BESTEST EPW 를 `_data/weather/` 에 넣지 말 것.** `ep_simulator.py:853~` 자동
탐색이 한국 프로젝트에 Denver 를 물릴 수 있다. 테스트 디렉터리 안에 격리해뒀다.

**Tier B 를 막는 것 5개** (README 에 상세):
🔴 `idf_builder.py:317` IdealLoads 가 외기를 항상 물음(140 은 기계환기 0) + 습도제어 기본 적용
🔴 `ep_simulator.py:188` WWR 중앙 스케일 창 → 3m×2m 두 짝 표현 불가
🔴 `idf_builder.py:94,164` `SolarDistribution`=`FullExterior` 고정(600 은 `FullInteriorAndExterior`)
🟡 `ep_simulator.py:1242` `add_infiltration()` 인자 없이 호출
🟡 `ep_simulator.py:853~` 기상 강제 지정 경로 없음

**다음**: ① stock IDF 출처 정리(OpenStudio CLI 판단) ② 케이스 확장(기준값 92행은 이미 있음,
IDF 만 확보하면 됨) ③ **케이스 간 델타 검사**(600→900 열용량 / 600→610 차양 / 600→620 창방위 —
델타는 기상·단위·절대 스케일에 둔감하고 물리 번역만 때리므로 절대값보다 예민하다) ④ Tier B.

codex 검토 진행 중: `.relay/ashrae140-tier-a-review.md`

---

### 다음에 바로 할 일

작은 것부터 적었다. 1~3은 이번 검토에서 확정된 것이고, 4 이하는 이전부터 밀린 것이다.
(6번 ASHRAE 140 은 Tier A 완료 — 위 절 참조)

**1. 🟢 저신뢰 면적 폴백을 결과에 기록** — 가장 작다, 즉시 가능
   선언 면적이 없으면 Air/Ceiling 기반 윤곽을 부하 기준 면적으로 쓰는데,
   그 사실이 결과 어디에도 남지 않는다. 개방 계단실의 개방부 면적은 실제
   사용 바닥면적과 다를 수 있다. 결과 리포트에 provenance(floor / air_or_ceiling /
   multi_patch)와 "저신뢰 폴백 사용" 을 assumption 으로 기록할 것.

**2. 🔴 모델 의미 검증 규칙** — 기하 검사보다 우선순위가 높다
   지금은 기하·참조 오류만 잡고 "실행은 되지만 의미가 틀린 상태"를 못 잡는다.
   EPlusSimple `debug.py` 참고: 존에 floor/ceiling/wall 최소 하나씩 존재 /
   창·문과 투명·불투명 construction 호환성 / 비외기창 blind / 설비 참조 호환성 /
   미사용 설비·구조체 / 냉난방·급탕 시스템 부재.

**3. 🔴 `/api/simulate` payload validator 완성**
   현재 미반영: 전역 ID·XML 참조 타입·단위 불일치·not_enclosed·Surface 자체의
   퇴화 좌표·opening 퇴화·잘못된 adjacentZone.
   (중복 zone id·빈 id·면적 필드·geometric drift·baselineModel 은 반영됨)

**4. 🟡 다중 레벨 지오메트리** — codex 는 2D union 보다 이쪽이 먼저라고 했고 동의한다
   "최저 Z 수평면 = 바닥" 정의가 메자닌·복층·split-level·경사바닥·피트에서 깨진다.
   **선언 면적이 없으면 부하 면적을 통째로 잃는다** — 둘레 왜곡보다 심각하다.
   수평 후보를 Z tolerance 로 그룹화 → 레벨별 2D union → floor plate 구분 →
   경사면은 수평 투영으로 별도 처리 → 작은 피트가 주 바닥을 대체하지 않도록 면적 비중 사용.

**5. 🟡 Shapely 도입 여부 판단**
   내부 edge 상쇄는 T-junction·부분중첩·hole 을 처리 못 한다. codex 는 "독자 구현은
   불가능에 가깝다"며 검증된 라이브러리를 권한다. **의존성을 피하려던 내 판단을
   바꾸는 게 맞다고 본다** — 다만 Docker 배포 제약 확인이 선행.

**6. 🟡 ASHRAE 140/BESTEST 자동 pass/fail 벤치마크**
   양쪽 프로젝트 모두 없는 항목이다. EPlusSimple 의 "모델×프로필×기상 일괄 실행"
   구조는 참고하되 **승인 기준값 + tolerance assertion** 을 반드시 붙일 것.

**7. 🟡 냉방 차단을 35℃ setpoint 대신 availability schedule 로**
   PTHP 는 유닛·팬·냉방코일·난방코일·보조히터가 `op_schedule` 하나를 공유하므로
   계절 마스크를 그대로 얹으면 **겨울 난방까지 꺼진다.** 냉방코일에만 별도
   스케줄이 필요하고, IdealLoads 폴백은 35℃ 를 먼저 빼면 계절 차단이 사라진다.

**8. ⚪ opening containment·overlap 검증 / 진짜 watertight 검사**
   현재 opening 은 면적 합만 본다(평면 이탈·겹침 통과). `detect_zone_gaps()` 는
   법선 잔차만 보므로 "폐합 실패율"로 해석할 수 없다(edge incidence + signed volume 필요).

**9. ⚪ 사용자 안내 문구 정교화**
   "BIM 수정 불필요"는 유지하되 **"파일에 아무 문제 없다"로 오인시키지 말 것.**
   개방 Air 경계 때문에 독립 검증 못 한 지점, 자기참조 최하층 바닥 19면의
   경계조건 미확정을 함께 알려야 한다.

**10. ⚪ 프로필 DB 확장(11→24종) / 프런트 code splitting / CI 품질 게이트 / 커밋 정리**
   `용호동 파일 2.xml` 은 여전히 untracked — 실제 파일 검증 테스트가 다른 환경에서 skip 된다.

---

### 태양광(PV) 모델링 — 3라운드 검토 완료 (2026-08-02)

**현재 상태**: `pvCapacity`(kW) 하나만 받아 연 1,300 kWh/kW 를 곱하고 9~17시 균등 분배.
`Generator:Photovoltaic` 등 EnergyPlus PV 객체가 **하나도 없다.**
지역·경사·방위·차폐가 결과에 전혀 반영되지 않는다.

**민감도(codex 추정)**: 지역 ±10~20% / 방위 10~25% / 남향수직 BIPV 25~45% 감소 /
**북향수직은 50% 이상 과대평가**. 발전량 20% 과대면 단순회수기간이 약 25% 짧아 보인다.

**합의된 계획 — 1+2단계를 한 릴리스로**
1단계만 내보내면 수지 구조는 좋아져도 평탄한 1,300 곡선이 정밀한 결과처럼 보인다.

- **1단계** 공통 시간별 물리량: `pv_dc_generation`, `pv_ac_generation`, `self_consumed`,
  `grid_import`, `grid_export`, `curtailed_or_uncredited`, 적용 정책 식별자.
  ⚠️ **요금과 ZEB 가 같은 값을 쓰도록 강제하면 안 된다** — 정책 기준이 다르다
  (요금=자가소비+보상 역송 / ZEB=인증기준상 총생산량). 원천만 공유하고 집계는 분리.
- **2단계** `Generator:PVWatts` → `ElectricLoadCenter:Generators` →
  `Inverter:PVWatts` → `Distribution`. 결과에 `EnergyPlus 25.2 / PVWatts V5` 메타데이터.
- **3단계** 3D 면 선택 + 형상 결합. `Simple` 고정 금지(음영은 낫지만 온도 모델이 단순) —
  Sandia/One-Diode 또는 면별 입사일사를 검증된 후처리에 연결.
  **개략/상세 모드를 명시 구분**하고 조용히 교체하지 않는다.

**평지붕 랙**: "경사 표현용 가상 면"과 "실제 차양 형상"을 구분한다. 후자는 패널 높이·
행 간격·치수가 입력돼야 하고 그때만 지붕 음영(냉방↓·난방↑)을 반영한다. 2단계는 Decoupled.

**검증 계층**: 회귀(고정 IDF/EPW 기준출력) → 구현(수동 기준 IDF 대조) →
교차모델(pvlib/SAM, 차이 원인 기록) → 실측. **웹 PVWatts V8 을 V5 의 정답으로 삼지 말 것.**
pvlib 은 golden CSV 생성용 **테스트 전용** 의존성.

**최소 UI 4항목**: DC kW / 설치 유형 프리셋(평지붕 랙·경사지붕·벽면) /
경사·방위(프리셋 자동, 고급에서 수정) / 잉여 처리(무보상·상계·매전).

**전제 정정**: gbXML 파서는 `direction` 을 "Roof" 로 덮고 **경사를 파싱하지 않는다**
(법선벡터에서 유도 필요). **Shade 파싱 구현이 없다**(주변 차폐 활용 불가).
`solarHelper.js` 는 균시차·경도를 무시한 표시용 근사라 발전량에 재사용 금지.
EPW 는 `_data/weather/` 에 80개 있고 git 추적 중이다(전제 성립).

**내 판단이 필요한 미해결 4건**
1. 잉여 처리 기본값 → **무보상을 기본으로 두되 화면에 노출.** 과대평가보다 과소평가가 안전하다.
2. 과도기 경고 → **즉시 넣는다.** 라벨 하나이고, 지금 사용자가 이 수치로 투자 판단을 한다.
3. 기존 프로젝트 → 결과에 모델 버전을 남기고 **조용히 재계산하지 않는다.**
   "구 모델로 계산됨" 표시 + 재계산 동의.
4. 전체 우선순위 → **검증 작업(의미 검증·payload validator)이 PV보다 먼저.**
   검증 결함은 모든 프로젝트에 조용히 영향을 주지만, PV 오차는 PV 를 쓰는 프로젝트에만
   영향을 주고 방향(과대)도 알려져 있다.

### 미해명 — 난방 과소산정 (2026-08-04 갱신)

⚠️ **지표를 반드시 구분할 것.** 아래 표의 "부하"와 "소비"는 다른 값이다.
예전 기록의 6.6~7.0 / 19.9~20.7 은 **연료·전력 소비** 기준이다.

| 침기 | 난방부하 | 난방연료 | 냉방부하 | 냉방전력 |
|---|---|---|---|---|
| legacy AFN | 6.43 | **6.64** | 57.11 | **20.64** |
| 고정 0.3 ACH | 3.28 | 3.41 | 57.85 | 20.78 |
| **고정 0.5 (현재 기본)** | 8.87 | **9.21** | 53.87 | **20.05** |
| 고정 1.0 ACH | 29.81 | 30.98 | 46.41 | 18.52 |

legacy 의 6.64 / 20.64 가 예전 기록과 일치한다. 침기 모델 수정으로 난방연료가
**6.64 → 9.21 kWh/㎡ (+39%)** 로 올랐고 냉방전력은 거의 변하지 않았다.

**지워진 후보**
- 면적 기준 불일치 (수정, 비율 불변)
- 지면 열손실 누락 (Ground 승격 민감도 +0.4, 6.1% — 기각)
- 기상 파일 오선택 (성남→서울 정정, 기후 차이 작음)
- **내부발열 과다 — 기각. 내 단위 오류였다.** ⬇︎
- **콘센트 부하 이중계산 — 용호동에는 해당 없음.** ⬇︎

#### ❌ 내부발열 44.5 W/㎡ 는 틀린 서술이었다 (단위 혼동)

"조명 19.4 + 기기 25.1 = 44.5" 는 **W/㎡ 가 아니라 kWh/㎡·년** 이다.
실제 값:

| | 값 |
|---|---|
| 설치 밀도(면적가중) | 조명 **8.0** + 기기 **9.8** = **17.7 W/㎡** |
| 연간 실측(스케줄 적용) | 44.5 kWh/㎡·년 |
| 연평균 환산 | **5.07 W/㎡** |

검산: 17.7 W/㎡ × 8760h = 155 kWh/㎡(상시 가동 시) → 실측 44.5 는 스케줄 적분
**28.7%**. 사무소 주간운영으로 타당하다. **용호동 내부발열은 과대하지 않다** —
사무소 아키타입 기본값(조명 9 / 기기 12)이 그대로 적용된 정상 범위다.

#### ❌ 콘센트 이중계산 — 용호동에는 없다 (다른 파일에는 위험이 남는다)

용호동 20개 존 전부 `outletCount = None` 이라 콘센트 부하가 더해지지 않았다.
조명·기기는 아키타입 값 그대로다(사무실 9/12, 화장실·계단 4/1).

**다만 위험 자체는 실재한다**: `outletCount` 가 있는 파일에서는
`equipment = 아키타입 12 + calc_outlet_power_density()(최대 25) = 최대 37 W/㎡` 가 된다
(`outletLoadType` 기본값이 `"sum"` — App.jsx:507, 629, FloorEditor.jsx:685).
**아키타입 12 W/㎡ 가 이미 일반 콘센트를 포함한 값이라면 이중계산이다.**
아키타입 정의를 확인해 `sum` / `max` / 명시값 우선 중 무엇이 맞는지 정해야 한다.

**남은 후보**: 외피 성능. 단열재가 광물면 R13(λ=0.062)·광물보드 R-10.4 두 종뿐이고
창이 15개(WWR 매우 낮음)다. 다만 ASHRAE 140 Tier B 에서 외피 전도·열용량·창 일사·
차양·방위 번역이 델타까지 NREL 과 일치했으므로(0.5% 이내), **번역 오류보다는
입력값(재료 물성·U값)의 타당성**을 봐야 한다.

**그리고 침기가 지배적이다**: 0.3~1.0 ACH 라는 평범한 불확실 범위에서 난방연료가
3.41 → 30.98 kWh/㎡ 로 **9배** 움직인다. 기밀성 실측 없이는 난방 결과에 의미 있는
정밀도를 주장할 수 없다.

---

### 정정된 과거 기록 (아래 옛 항목과 충돌하면 이쪽이 맞다)

- ESLint `motion` 오탐: "건드리지 말라"고 적어둔 것은 진단이 얕았다. 원인은
  **`eslint-plugin-react` 미설치**였고 설치 후 해결. 현재 lint 0 errors / 0 warnings.
- 바닥면적 3중 불일치(864/964/1050): 해결됨. `<Space><Area>` 선언값 우선으로 864.38㎡ 단일 기준.
- "EPlusSimple 에 ASHRAE 140 표준 벤치마크 대조가 있다": **틀렸다.** 기준 범위 비교도
  실패 assertion 도 없다. 양쪽 모두 공인 기준값 대조가 없는 것이 정확한 대비다.
- 면적 경고 10건: **오탐이었다.** 층간 슬래브 이중계산 + 단순 10% 임계값 탓이다.
  중심선 기준 export 는 정상 관행이고 함축 offset 0.031~0.190m 는 전부 현실적이다.

---

## 2026-08-02 — 릴레이 도구 정비 (Gemini 중재 도입)

**모델 역할을 실측으로 확정**했다 (상세는 메모리 `relay-model-roles`).

| 역할 | 담당 | 근거 |
|---|---|---|
| 코드 검토 | codex | 저장소를 읽고 file:line 근거 제시 |
| 단일 문서 압축·분류 | gemma4:e4b (로컬) | 20,662자까지 성공, 한도 안 씀 |
| 문서 간 상태 대조(중재) | **Gemini CLI** | gemma 가 두 번 실패한 과업을 1회 처리 |

**gemma 를 중재에 쓰지 말 것.** 13,433자 입력에서 600초 타임아웃 2회.
크기 문제가 아니다 — 같은 모델이 20,662자 단일 문서 요약은 성공했다.
중재는 "round 1 지적이 round 2 에서 반영됐는지" 추적하는 **추론** 과업이다.

**Gemini CLI 주의**: 개인 무료 티어(oauth-personal)는 `IneligibleTierError` 로 거절된다.
AI Studio API 키를 쓸 것. 헤드리스 호출에 `--skip-trust` 필요.

### 이번에 낸 사고와 교훈

1. **다른 Claude 세션에 명령을 보냈다.** "codex 도 ollama 도 아니면 빈 셸"이라는
   내용 기반 추측으로 패널을 골랐다가 오인했다. 파일 변경은 없었지만 남의 세션
   흐름을 끊었다. → 전용 패널을 직접 만들고 **제목으로만** 식별하도록 교체.
2. **한도 감지 오탐 3연속.** 맨 숫자 `429`(커밋 해시 `ef429e3` 에 걸림) →
   `한도` 단독 → `try again later`(503 일시 과부하도 이렇게 말함. CLI 가 자체
   백오프 재시도 중인 로그를 보고 중단시켜 성공했을 호출을 죽였다).
   기준: **기다려도 안 풀리는 것만** 넣는다.
3. **중재 이력을 최신 질문+전체 codex 로 나열**해 gemma 가 해결된 항목 3건을
   쟁점으로 올렸다. → 시간순 교차 배치(Q1→C1→Q2→C2)로 교체.
4. **스크롤백 절대 행 오프셋**이 버퍼 상한에서 무효화돼 긴 답변을 통째로 놓쳤다.
   → 논스 마커가 있으므로 오프셋 자체를 제거.

패널에서 실행하되 **결과는 파일로 회수**한다(stdout/stderr/종료코드 리다이렉트).
가시성과 안정성을 함께 얻는 구조다.

## 2026-07-30 — EPlusSimple(SNU) 대비 장단점 (codex 판정 + gemma 정리)

`891694b` 기준. codex가 두 저장소를 읽고 판정, gemma가 정리. **아래는 내가 코드로
재확인한 것만 확정 사실로 적는다.** 미확인 항목은 그렇게 표시했다.

### 먼저 바로잡은 내 오해 3건

1. **"EPlusSimple에 ASHRAE 140 표준 벤치마크 대조가 있다"는 틀렸다.**
   `scripts/dev/regressiontest.py` 는 `ASHRAE 140 modified.grm` 을 프로필·기상별로
   반복 실행해 결과 표를 LaTeX에 삽입할 뿐이다. **기준 범위·기준 프로그램 결과와의
   허용오차 비교도, 실패 assertion도 코드에 없다.** 표준 적합성 시험이 아니다.
   → 정확한 대비: 저쪽은 "시스템 수준 smoke/report 도구", 우리는 "자동 판정 테스트 116개".
   **양쪽 모두 공인 기준값 대조는 없다.**
2. **저쪽 입력 검증은 Excel 전용이다.** `debug.py` 의 `JSON_INSPECTORS` 는
   `JSON_INSPECTORS = []` 로 **비어 있다**(직접 확인). GRM/JSON 검증은 미구현이다.
   또 3단계는 항목별 등급이 아니라 "Exception 있으면 SEVERE, Warning만 있으면 WARNING"
   으로 병합하는 **결과 등급**이다. 우리 info/warn/choice/block(행동 정책 포함)과 동급 비교가 아니다.
3. **"우리 서버 신뢰 경계가 완성됐다"는 이르다.** `model_validation.py` 는 일부만 본다.
   특히 `zone_ids` 를 바로 set 으로 만들어 **중복 zone id 를 잃고 있었다**(확인 후 수정).

### 판정 요약

| 항목 | 판정 | 근거 |
|---|---|---|
| 입력 접근성(BIM 연계·웹) | **우리 장점** | gbXML 업로드 후 웹 3D 편집. 저쪽은 Excel/GRM 준비 + Windows launcher |
| 입력의 명시성·통제성 | 저쪽 장점 | 구조화 Excel은 관계를 명시. gbXML은 자기참조·면 귀속·이름 기반 용도추론 등 모호성이 구조적 |
| 기하·단위 검증 | **우리 장점** | 전역 ID·XML 참조·면적 단위·퇴화 기하·opening 초과·자기참조·선언/기하 불일치 |
| **의미 계층 검증** | **저쪽 장점** | 존에 floor/ceiling/wall 부재, 잘못된 인접존, 설비 참조·호환성, 투명/불투명 construction, 비외기창 blind, 미사용 설비, 냉난방·급탕 부재 |
| 경고 UX·승인 정책 | **우리 장점** | choice/block이 실제 행동(진행 차단, Ground/Adiabatic 선택)으로 연결 |
| HVAC 모델 상세도 | **우리 단점** | 저쪽은 Chiller/AbsorptionChiller/Boiler/AHU/FCU/Radiator/RadiantFloor + PlantLoop. 우리는 존 단위 PTHP·UnitHeater+WindowAC·IdealLoads |
| 프로필 DB | **우리 단점** | 저쪽 24종(환기량·급탕·요일·방학 포함) vs 우리 11종 |
| 설정온도 | 무승부 | 양쪽 20/26℃ 기본, 양쪽 override 가능 |
| 계절 제어 | 제한적 우리 장점 | 우리만 냉방기간 마스크. 단 35℃는 절대 차단이 아니고 고정 기간이 이상고온에 부적절할 수 있음 |
| 면적·내부발열 일관성 | **우리 장점** | 선언면적 우선 + 용도별 부하 전달이 코드상 일관 |
| 1차에너지·CO2 계약 | **우리 장점** | 백엔드 단일 출처 + 회귀 테스트로 고정 |
| 단위 테스트 | **우리 장점** | pytest 116개(현재 118). 저쪽 저장소에 pytest/unittest suite 없음 |
| **물리적 결과 검증** | **무승부이자 양쪽 약점** | 양쪽 모두 공인 기준값 assertion 없음 |
| 시스템 수준 시나리오 실행 | 저쪽 장점 | 프로필 24 × 지역 18 조합을 실제로 돌려 관찰 |
| 문서 | **우리 단점** | 저쪽 TRM/RN/RTR LaTeX 본문 + 도식·참고문헌. 우리는 WORKLOG·README 수준 (방법론 문서는 저장소 밖 아티팩트) |
| 비용·리모델링 의사결정 | **우리 장점** | 공사비 DB·LCC·요금·CO2·PV·baseline 비교·추천 UI 결합 |

### 당장 가져올 것 3가지 (우선순위)

1. **모델 의미 검증 규칙** — 저쪽의 `InsufficientSurfaceForZone`,
   `InvalidFenestrationConstruction`, `BlindForNonOutdoorWindow`,
   `InvalidSourceSystemName`, `NoHVACSystemApplied` 에 해당하는 검사.
   **"실행은 되지만 의미가 틀린 상태"를 잡는다.** 기하 검사 개수를 늘리는 것보다 우선순위가 높다.
2. **pass/fail 있는 벤치마크 harness** — 저쪽의 모델×프로필×기상 실행 구조는 가져오되
   LaTeX 표 생성만 복제하지 말고 **승인 기준값 + tolerance assertion**을 붙인다.
3. **한국형 프로필 확장** — 24종의 환기량·급탕·요일·방학 필드 참고. 단순 복사 금지,
   출처·단위·적용범위를 검증하고 CSV/JSON 으로 코드 밖에 분리.

상세 PlantLoop 이식은 가치는 크지만 "당장"이 아니다 — 데이터 모델·UI·비용집계·결과 미터를 함께 바꿔야 한다.

### 우리 단점 분류

**구조적으로 어려운 것**
- gbXML 익스포터별 의미 손실(자기참조 바닥, 공유면 한쪽 귀속, 이름 기반 용도추론) —
  원본 BIM 정보 없이 완전 자동 복구 불가
- 상세 HVAC/PlantLoop 확장 — 백엔드·UI·IDF 생성·결과 집계 전면 재설계 필요
- 실제 건물 calibration — 계측·운전·제어 데이터 없이는 코드로 해결 불가
- 웹 서비스의 EnergyPlus 계산 자원(큐·격리·메모리·보안)

**작업하면 되는 것**
- ASHRAE 140/BESTEST 자동 pass/fail 벤치마크
- 의미 검증 규칙 추가
- `/api/simulate` payload validator 완성 (전역 ID·참조 타입·단위·not_enclosed 미반영)
- 프로필 DB 확장 + 출처 문서화
- TRM/RN/검증 보고서
- **냉방 차단을 35℃ setpoint 대신 명시적 availability schedule 로 전환**
- opening containment·overlap 검증
- 프런트 번들 code splitting
- Linux/CI 품질 게이트(pytest+lint+build+benchmark)

**최종 평가**: 우리는 BIM 업로드·오류 진단·웹 편집·비용 의사결정·회귀 테스트에서 우세.
저쪽은 설비 모델 폭·한국형 프로필·의미 계층 검증·기술문서·시스템 시나리오 실행에서 우세.

---

## 2026-07-28 — 용호동 전체 시뮬레이션 + 결과 교차검증

자기참조 수정 후 처음으로 실제 업무 파일을 끝까지 돌렸다. **파이프라인은 완주**했고
경고가 API 응답까지 전달되는 것, 자기참조로 남은 면이 0개인 것을 확인했다.

결과: 요구량 117.6 / 소요량 80.1 / 1차 126.7 kWh/㎡·년, CO2 35.33 kg/㎡·년,
연간 에너지비 1,054만원. 월별 합계는 난방 5.1 / 냉방 21.6 / 조명 21.8 / 기기 28.5 / 급탕 2.4.

**난방 5.1 vs 냉방 21.6 은 서울 사무소로서 비상식적이다.** 원인 후보를 codex에 검토시켜
아래를 코드로 확인했다.

- ❌ **내 해석 오류**: "11~4월 냉방 0.0 이니 `COOLING_OFF_TEMP` 마스크가 작동한다"는 틀렸다.
  `cost_analyzer.py:804-810` 이 월별 값을 `round(..., 1)` 로 반올림하므로
  0.05 kWh/㎡·월 미만은 전부 0.0 으로 찍힌다. 0.0 은 차단의 증거가 아니다.
- ✅ **바닥면적 3중 불일치** — 위 「다음에 바로 할 일」 0번. 1,050.42 / 964.47 / 864.37 전부 실측 재현.
- ✅ **Z=0 자기참조 바닥 6면 = 177.99㎡** 가 Adiabatic. 지면 열손실 누락의 유력 후보.
- ⚠️ **`App.jsx:490-492` 가 모든 존에 `peopleDensity=0.1 / lightingPower=10 / equipmentPower=15`
  를 주입**한다(608-610은 `z.x || 기본값`). 화장실·계단실에도 사무실 수준 부하가 들어간다.
  **단, 이번 실행은 API 직접 호출이라 프론트를 거치지 않았다** — 존에 `activityId` 만 있고
  부하값은 비어 있어 백엔드가 용도별 아키타입을 썼다. 즉 이 버그는 실재하지만
  **이번 난방 과소산정의 원인은 아니다.** (프론트 경로에서는 별개로 고쳐야 한다.)

난방 과소산정의 최종 원인은 아직 미확정. 지면 열손실 누락과 면적 불일치가 유력하다.

## 2026-07-26 — 멀티 에이전트 릴레이 검토 파이프라인 구축

**한 일**

- 로컬 서버 기동 확인: 백엔드 `127.0.0.1:8000` (uvicorn, 루트 `.venv`),
  프론트 `localhost:5173` (Vite 8.0.3). 5173을 잡아서 CORS 문제 없음.
- Codex CLI 설치 — `/usr/local/lib` 는 EACCES라 `npm i -g --prefix ~/.local @openai/codex`.
  `codex-cli 0.145.0`, ChatGPT 계정으로 로그인 완료.
- **릴레이 파이프라인 2종 구축** (상세는 메모리 `cmux-relay-pipeline` 참조)
  - `scripts/relay.sh` — 헤드리스. codex CLI 직접 호출.
  - `scripts/relay-cmux.sh` — cmux 패널 가시화. Claude → codex 패널 → ollama 패널 → Claude.
    진행 상황이 화면에 그대로 보인다. 종료코드 0=P0없음 / 10=P0있음 / 20=라운드상한(3).
- **분류 모델 실측 비교** → `gemma4:e4b` 채택.
  | 모델 | 소요 | 원문 보존 |
  |---|---|---|
  | gemma4:e4b | 328초(첫 로드 포함) | 4/4 ✅ |
  | llama3.2:3b | 5초 | 0/4 — 본문을 "원문"이라는 글자로 치환 ❌ |
  | qwen3.5 | 2회 완주 실패 | — ❌ |
- 파이프라인 자체에서 버그 5개를 잡아가며 완성. 마지막 실행 EXIT=10 정상.

**관측 — `용호동 파일 2.xml`** (저장소 루트, 아직 git 미추가)
서울, Office, 연면적 864.37㎡, 5층, Space/Zone 20, Surface 235, 창 15.
시뮬레이션 시 주의할 점 2가지:
- 지면 접촉면(`SlabOnGrade`/`UndergroundWall`)이 **하나도 없다** → 1층 바닥 열손실 왜곡 가능.
- 외벽 113면에 창 15개뿐이라 WWR이 매우 낮다 → 창호 개선 시나리오 효과가 과소평가될 수 있다.

**미해결로 남긴 것**
- 위 P0 2건 (맨 위 「다음에 바로 할 일」 참조).
- 릴레이를 Claude Code `Stop` 훅에 걸어 자동 트리거하는 건 아직 안 붙였다.
